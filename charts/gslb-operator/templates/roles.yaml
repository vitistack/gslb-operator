apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gslb-operator.fullname" . }}-policy-roles
data:
  policy.yaml: |
    roles:
      {{- with .Values.policy.roles }}
        {{- toYaml . | nindent 4 }}
      {{- end }}
